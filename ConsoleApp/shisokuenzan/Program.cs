class Program
{
    static void Main(params string[] args)
    {
        // コンソールに演算子を入力。
        Console.WriteLine("演算子を一つ入力してください。");
        var enzanshi = Console.ReadLine();
        Console.WriteLine("数値をスペース空けて入力してください。");
        var baseValue = Console.ReadLine(int)();
        var lastValue = 0;
        var middleValue = 0;
        if (enzanshi == +)
        {
            foreach (var value in baseValue)
            {
                lastValue += value;
            }
        }
        else if (enzanshi == *)
        {
            for (i = 1; i <= baseValue.length;)
        }




        // 数字を複数入力し、その数値に対して先ほどの演算子を適用
        // 適用結果をコンソールに表示する
    }
}